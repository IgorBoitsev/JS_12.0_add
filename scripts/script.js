'use strict';

let isNumber = function(n) {
  return !isNaN(n) && isFinite(n);
}

alert('Я загадал число от 1 до 10.\nПопробуй угадай его!');

let guessNumber = function() {
  let n = +prompt('Твой вариант: ');
  
}


